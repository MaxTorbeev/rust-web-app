[package]
name = "auth"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
axum = "0.8.9"
api-response = { path = "../api-response" }
tracing = "0.1.44"
argon2 = { version = "0.5.3", features = ["std"] }