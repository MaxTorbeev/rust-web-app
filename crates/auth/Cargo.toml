[package]
name = "auth"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
axum = "0.8.9"
api-response = { path = "../api-response" }
log = "0.4.32"
tracing = "0.1.44"