[package]
name = "redis-lease"
version = "0.1.0"
edition = "2024"

[dependencies]
redis-client = { path = "../redis-client" }
thiserror = "2"

[dev-dependencies]
tokio = { version = "1.52.3", features = ["macros", "rt", "time"] }
