[package]
name = "event-bus"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio-events = "0.3.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
uuid = { version = "1", features = ["v4", "serde"] }
