[package]
name = "redis-client"
version = "0.1.0"
edition = "2024"

[dependencies]
redis = { version = "0.32", features = ["tokio-comp", "sentinel", "connection-manager", "script"] }
support = { path = "../support" }
thiserror = "2"
