[package]
name = "nats-client"
version = "0.1.0"
edition = "2024"

[dependencies]
async-nats = { version = "0.50", default-features = false, features = ["ring"] }
bytes = "1"
thiserror = "2"
futures-util = "0.3"